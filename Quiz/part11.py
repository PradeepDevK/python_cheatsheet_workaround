str1 = 'awesome'
print(str1.replace('e', 'r', 1))

# awrsome
# In the replace() method:

# 'e' is the substring to be replaced.
# 'r' is the replacement string.
# 1 is the count parameter, specifying that only the first occurrence of 'e' should be replaced.