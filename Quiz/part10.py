my_list = [2, 3, 1]
print(my_list.pop(1))

# returns 3
# my_list = [2, 3, 1]: This line initializes a list my_list containing the elements [2, 3, 1].
# my_list.pop(1): This line removes the element at index 1 from my_list, which is 3. The pop() method returns the removed element, so 3 is returned.